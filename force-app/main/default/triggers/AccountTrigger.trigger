/*trigger AccountTrigger on Account (before insert, before update, after insert, after update) {
   /*
    if (trigger.isBefore && trigger.isInsert) {
        System.debug('Account before insert triegger called');
   }
   if (trigger.isAfter && trigger.isInsert) {
    System.debug('Account after insert triegger called');
   }
   if (trigger.isBefore&&trigger.isUpdate) {
        System.debug('Account before update tr called');
   }
   if (trigger.isAfter&&trigger.isUpdate) {
        System.debug('acc after update t called');
   }
   */
    ///System.debug('Account before INSERT trgr Called');
   //System.debug('Account before UPDATE trgr Called');
   
   /* if(Trigger.isbefore){
    System.debug('Account before insert trigger called');
    }   
    if (Trigger.isAfter){
        System.debug('Account AFTER insert trigger called');
    }

    if (trigger.isUpdate) {
        System.debug('Account before UPDATE trgr Called');
    }
    if (trigger.isInsert) {
        System.debug('Account before insert trigger called');
    }
    */
}