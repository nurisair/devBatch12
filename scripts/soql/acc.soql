SELECT Id, Name FROM Account ORDER BY CreatedDate DESC LIMIT 2;

SELECT id, name FROM Account


SELECT Id, Name, Priority__c, Status__c, Subject__c FROM Salesforce_Ticket__c where status__c != '' and priority__c in ('high','medium');

SELECT Id, Name, Priority__c, Status__c, Subject__c FROM Salesforce_Ticket__c where status__c != '' and (priority__c ='high' and priority__c='medium');